const assert = require("assert");
const { Given, When, Then } = require("cucumber");

Given("user is in auth page", function() {
  return "pending";
});

When("user enters email and passawork and clicks submit button", function() {
    return "pending";
});

Then("user is authorized and redirects to tredning page", function(expectedAnswer) {
    return "pending";
});